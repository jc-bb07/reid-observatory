# Islands Observatory — BUILD LOG (session 14 Jul 2026)

## Session goal
Linked suite of single-file HTML dashboards comparing healthcare across IoM, Jersey, Guernsey, NHS Orkney, NHS Shetland, NHS Western Isles (+ England/Wales/Scotland baselines). Three audience lenses (public / politician / management), every number source-traceable.

## Phase 1 — Extraction sprint (done)
New tracker-format CSVs written & python-validated:

| File | Rows | Statuses |
|---|---|---|
| `reviews-inspections/nhs-western-isles/his-wih-inspection-2024_requirements.csv` | 22 | all `identified` (Jan 2026 follow-up report NOT yet collected — top collection priority) |
| `reviews-inspections/nhs-western-isles/his-wih-mental-health-2026_requirements.csv` | 17 | all `identified`; repeat-theme cross-refs to 2024 acute reqs noted |
| `reviews-inspections/nhs-orkney/his-balfour-maternity-2026_requirements.csv` | 11 (9 req + 2 rec) | all `identified` |
| `reviews-inspections/nhs-orkney/audit-scotland-aar-2024-25_recommendations.csv` | 6 | statuses at 17 Jun 2025: 3 done, 1 partial, 2 in-progress (matches report's "3 implemented/superseded, 3 outstanding") |
| `reviews-inspections/jersey/jao-governance-2018_recommendations_with_2022_status.csv` | 22 | completed 6 / partial 12 / accepted-not-implemented 4 — **reconciles exactly** with JAO follow-up summary (6/12/4) |
| `reviews-inspections/jersey/cag-staff-deployment-r7-2023_recommendations.csv` | 23 (21 R + 2 P) | all `unknown` — no published C&AG follow-up in library |
| `reviews-inspections/guernsey/guernsey_review_index.csv` | 4 | index/findings — Guernsey has no extractable rec-tracker corpus (that IS the finding) |
| `benchmarking/02_extracted_data/finance/orkney_nhs_finance.csv` | 9 | Orkney AAR finance exhibits (deficit plan £5.8m → outturn £3.874m, £9m brokerage, savings 4.07/4.0, 3-yr cum deficit £13.9m, Level 3 escalation) |

Decisions:
- Status vocab mapping for JAO follow-up: Implemented→`completed` (incl. 2× "implemented but scope to improve", noted), Partially→`partial`, Not implemented→`accepted` (all 22 were accepted by Government in 2018 — "accepted but not done" is the honest status).
- R.148/2023 leadership review contains **no formal recommendations** (Chief Officer self-review concluding HCS not over-managed) — recorded as a review with 0 actions, note shown.
- Guernsey SMC handover treated as a review-index: 4 reviews in the 2020-25 term (none health), 25 hearings (2 with HSC). Gedze reviews 2021/2022 unpublished → FOI stubs.
- HIS reqs get status `identified` (issued, no follow-up evidence in library yet).
- Non-IoM actions carry **no impact score** — impact scoring (scope×risk×urgency) exists only for the curated IoM set; inventing scores for 180+ extracted recs would be false precision. UI hides the badge when absent.

## Phase 2 — Data layer (done)
`70_dashboards/data/observatory_data.js` (192KB, node --check clean), generated by script from source CSVs/JSONs (no hand-typed numbers for series):
- `OBS.jurisdictions` — 6 islands + 3 baselines, populations with sources, headline AM cards.
- `OBS.am.series` — 9 series: IoM/Jersey/England/Wales/Scotland single-year; Guernsey + 3 Scottish boards 3-yr rolling (from `island_boards_avoidable_series.json` / `avoidable_mortality_data.json` / NRS CSVs). `mode` field marks basis; comparability text embedded.
- `OBS.am.split` — preventable vs treatable latest (GSY/ORK/SHL/WIS 2022-24; ENG 2023; SCO 2024; IoM & Jersey = explicit gaps).
- Reviews/actions: IoM arrays copied **verbatim** from design reference (current to Jul 2026); 18 non-IoM reviews defined; 181 non-IoM actions loaded from CSVs (jsy 106, wis 39, shl 19, ork 17).
- Finance: Shetland AAR rows, Orkney AAR rows, CD budget-vs-actual (comparison_finance.csv), 9 narrative flags (IoM £32m over formula; Shetland £12.8m late allocations + DIA; Orkney Level 3/£9m brokerage; Jersey reserve draws; Guernsey thin reporting; WI AAR = gap).
- Premium: IoM tertiary £26.27m vs £22.05m plan; transfer spend FOI series; CAA cancellations 2018-2025 (deduped — source CSV had duplicated 2025 rows); GNAAS 63-mission log summarised by outcome; WI 93%/81% local-care shares; weather-data gap panel with 5 FOI targets.
- Gap registers both embedded.

## Phase 3 — Dashboards (done)
Six single-file HTMLs (~245KB each, data layer inlined; only external = Chart.js 4.4.1 cdnjs + Google fonts, same as design reference). Shared shell across all: design-reference CSS tokens copied verbatim; sticky topbar nav; audience lens switcher (localStorage `observatory-lens`, `body[data-lens]` CSS gating + JS re-framing); jurisdiction focus (localStorage `observatory-jur`); theme via `observatory-theme` + postMessage listener; opportunity engine + copyable FOI/PQ "What next" generators in shared runtime.

- `01_index.html` — hub: north→south island tiles (AM headline + hover trend popup), baseline strip, at-a-glance reviews/actions table, focus-jurisdiction Opportunities panel (lens re-orders by kind), links to all dashboards.
- `02_public-health.html` — all 9 AM series 2001–2024 on one linear-x chart (3-yr series dashed), ▶ Loop animation walking narratives per i
---

# Session 7 (14 Jul 2026) — MIAA disambiguation + Claude/Projects cataloguing

## MIAA: two reviews, not one (James challenged; verified correct but relabelled)
James read the Session-6 mcgov flip as double-counting one MIAA review. Verified against documents + contemporaneous reporting: there ARE two separate 2025 MIAA governance reviews —
1. **Commissioned by Manx Care** about its own Board (`miaa-governance-review-2025.pdf`, "Draft Final Report, July 2025", 62pp; media-reported 14 Aug 2025; 10 strategic + ~51 tactical actions) = `mcgov`.
2. **Commissioned by DHSC** (Jun 2025) on the arm's-length arrangement (GD 105/2025, laid Nov 2025, debated Dec 2025; four options; Option 3 chosen Feb 2026) = `miaa`. The GD 105/2025 document itself is NOT in the library (cited via Tynwald URL) — collection target.
Evidence: the library PDF contains none of the four-options / "not operated as intended" content; separate Manx Radio coverage 14 Aug vs 10 Dec 2025. Per James's ruling, relabelled by commissioner: **"MIAA Governance (Manx Care)"** and **"MIAA Governance (DHSC)"**, cross-referenced in both `what` texts (gen_data5.py now patches both entries). Rebuilt all 8 pages; suites: v05 **102/102**, v06 **47/47**, theme 13/13, compact 10/10. (Local verify_v05.js still had the stale tier-strip expectation from v0.6 — Session 6's edit hadn't landed in the library copy; fixed. verify.js/verify2.js are legacy ten-page suites and error on the retired filenames — superseded, do not use.)

## Duplicate purge (reviews-inspections)
Four byte-identical duplicate PDFs (uppercase names, copied 2 Jul from "Manx Care/Inspection and Review Reports/") deleted, lowercase canonical names kept: ManxCare_Governance_Review_2025 (=miaa-governance-review-2025), Adult_Learning_Disabilities_Review_CordinBright (=cordis-bright-adult-ld-2025), CQC_Inspection_Report_2022 (=cqc-inspection-acute-community-2022), Independent_Review_IoM_Health_Social_Care (=michael-review-2019). Source strings in `manxcare-governance-review-2025_strategic_actions.csv` re-pointed to miaa-governance-review-2025.pdf; catalog duplicate_notes updated.

## Claude/Projects cataloguing (~120 files surveyed; 32 filed, working artifacts left in place)
Filed to library (all logged in 00_catalog/data_catalog.csv/json): mandates 2021-22, 2022-23, 2024-25-revised + infographic, 2025-26 appendices 1–3; Assessment 2023-24 Figures; **ManxCare_5_Year_Retrospective.pdf** ("Five Years In Review", Jun 2026 — new from James mid-session); Operating Plan 2024-25 + Required Outcomes Framework 2022-23 draft; **dhsc-department-plans/** (new folder, 4 plans 2022-26); `_dhsc_docs_download_manifest.csv` (gov.im provenance); **explorer_topics/** (new folder, 8 Explorer topic JSONs schema 1.0 — acsc, amenable-mortality, antibiotic-prescribing, healthy-life-expectancy, mental-health-access, primary-care-access, stroke, ed — feeds roadmap item "Healthcare Explorer data unification"; cancer.json already at health-statistics-foi/); iom-gp-numbers-1999-2024.csv/json (extends GP series to 1999); IOM_Government_Workforce_Data.xlsx; IoMG_Real_Balance_5yr.xlsx; IoMG_Real_Balance_11yr.xlsx (**replaced** — library copy was a 1.9KB HTML error page, not an xlsx); 2 FOI drafts (avoidable-mortality causes, births/deaths D4E resubmission); RCS-FOI-ManxCare-General-Surgery.docx (→ reviews-inspections, relates to unpublished RCS review). NOT filed: dashboards/HTML, ps1/bat/hta fix scripts, App.jsx variants, logs, dev briefs (working artifacts, not data). Dup check was hash/size-based; library already held most ManxCare-DHSC-Data PDFs under underscore names.

## Session plumbing
Build/verify scripts had the previous session's VM name hardcoded — sed-replaced to current session (`intelligent-sharp-bell`); NEXT SESSION must do the same. /tmp/ob was left read-only by the old session: copy builders to `~/ob` instead and sed their `sys.path.insert` accordingly; `npm install jsdom` in ~/ob and run suites with `NODE_PATH=~/ob/node_modules`.

## Session 7 addendum — Figure 2 digitized
`workforce/iom_manxcare_wte_by_contract_group_2021-2026_DERIVED.csv`: monthly WTE by contract group (HMD/MPTC/NJC/PSC/M&C/JESP), Apr 2021–Feb 2026, digitized from the 5-yr retrospective's Figure 2 (454px raster; colour classification vs gridline calibration). Validated: Feb-26 total 3,063.9 vs published 3,062.42; HMD +93.6 vs stated +94; PSC +170.3 vs +172; MPTC +242.5 vs +251 (1px start error). NJC least reliable Dec22–Sep23 (overlaps HMD). README alongside. DERIVED per house rule 6; supersede via FOI for exact monthlies (also gets the Fig 4 CIP yearly split + Fig 5-7 monthly pay/agency/bank series, same request). Also extracted p28 finance table earlier → `finance/iom_manxcare_5yr_budget_expenditure.csv`.

## Session 7 addendum 2 — workforce composition panel on 06
Per James: derived WTE-by-contract-group series added to the data layer (**gen_data6.py**, extension 6, `OBS.mcWorkforce`) and a new panel added to 06_secondary-care.html (build_07.py, Workforce & flow section): three views (Composition stacked / Growth indexed Apr21=100 / Share %), stacked **bottom-up per James's ruling** (nurses, doctors, learning disability, social care, admin, directors), plain-English group names with contract codes in parens, share-view hover shows single group only, DERIVED chip + full caveat/defs/source line, published anchor (3,345 staff / 3,062.42 WTE Feb 2026), theme-aware redraw via obsOnTheme. All pages rebuilt with extension 6; suites: v05 102/102, v06 47/47, theme 13/13, compact 10/10; smoke on 06 clean. NB OneDrive→VM sync lag repeatedly served truncated build files this session — if a build fails with "unterminated string", compare `wc -c` against the host copy before debugging code.

---

# Session 8 (14 Jul 2026) — v0.7: James's work order

## Corrections
- **03 scoreboard publication ratio bug fixed**: numerator/denominator were computed over ALL reviews while the column displays ≥2019 only. Jersey 83%→**80%**, Guernsey 60%→**50%**; now shown as "N/M published, X%". Column headers trimmed ("Reviews ≥2019", "Actions" — parentheticals removed per James).
- **GP funding check (James: "£120 IoM vs £157 UK?")**: England 2023/24 average TOTAL NHS payment per registered patient = **£169.74** (NHS England Digital, pub. 29 May 2025, nominal, PCN categories removed); global sum alone ~£105–108/weighted patient. The "£157" is a total-payments-basis figure from an earlier year. Verdict: comparison basis matters more than the number — what IoM's £120 flat capitation covers is unpublished (FOI target). Anchor + explanation now on 05 (gen_data7 `gpEnglandAnchor` + fundingNote).
- **Representative wording**: hub public-swap no longer says "your MHK" for non-IoM focus — new shared `repTerm()` (MHK / Deputy or Connétable / Deputy / MSP or councillor).
- **Default lens**: confirmed already 'public' for first-time visitors (localStorage persists a user's own last choice thereafter — by design).

## 04 Financial — tabbed (v0.7)
Four filter-pill views, **Sources & Uses leads** (sankeys + pots + nine flags), then **Budget vs Actual** (NEW: IoM 5-year budget-vs-expenditure chart with % overrun rendered above each outturn bar — 2021/22 +3.7% … 2023/24 +9.9% … 2025/26 +4.1%, from `mcFinance5yr`/gen_data7 ex retrospective), **Savings** (Scottish boards chart + IoM ≈£42m 5-yr CIP panel; Fig 4 yearly split = collection target), **Per capita** (NEW: derived spend-and-budget per head, populations from JUR; Orkney = basis-not-comparable chip, WI = finance-gap chip; prim/sec/tert split flagged unpublished everywhere). **Board finance detail table retired** per James (finance CSVs remain in library). Charts redraw on tab switch (hidden-canvas sizing).

## 02 Public health — population health scorecard (NEW)
Six measures × 7 columns (six islands + England): life expectancy at birth (IoM m79.4/f84.2 2020-22; Gsy 83.6 2022-24), healthy life expectancy (IoM/Jsy RED not-published; Gsy/boards/Eng to extract), suicide ASMR (IoM 17.8 3-yr 20-22 · Jsy 7.0 · Gsy 9.6 closed-inquests · E&W 11.4 2024; basis warnings; Samaritans/Lifeline lines in public lens), infant mortality / MMR2-by-5 / cancer screening = chip rows with named sources (PHS, UKHSA COVER, NHSE screening, ScotPHO). Data: gen_data7 `popHealth` from library evidence (suicide-data.csv, explorer topic dataNotes).

## 07 + layout
Remoteness profile now **fits the page** (fixed-layout table, % widths) + two new rows: **Population served** (from JUR populations w/ census/NRS sources) and **Scheduled flight time to tertiary centre** (all "to collect" — airline timetables + board travel policies). 06 activity card rows now 6 equal columns, no overflow. "Questions this page raises" (06) retitled "What the numbers point to — analysis & open questions".

## Verification
v05 **102/102** (one expectation updated: remoteness rows 5→7, deliberate), v06 **47/47** (one updated: new pub-ratio format), theme 13/13, compact 10/10, smoke clean on all 8 pages.

## Answers logged for James
- Mental health coverage: present (06 CAMHS KPI, 07 MH off-island placements, mental-health-access.json in explorer_topics, suicide row on 02). **Sexual health: nothing held anywhere in the library — new collection target.**
- GP registrations time series: totals already in `gpSeries` (per-year registered patients, shown in 05 tooltips); **by-practice list sizes = FOI target** (not published).
- "Most beautiful dashboard" pass: fits/tabs/reframing done this session; full visual-polish pass (palette, motion, hero numbers) is a dedicated next-session job — flagging so it isn't lost.

## Deploy note
observatory_data.js changed (extensions 5-7) → redeploy ALL EIGHT files to public/islands/.


---

# Session 9 (14 Jul 2026) — v0.8 beauty pass (design-only)

Brief: DESIGN_BRIEF_v0.8.md ("the objectively most beautiful KPI dashboard…"), full scope incl. mobile per James. **Zero data changes** — gen_data*.py untouched, all numbers/sources/caveats/lens gating unchanged in meaning.

## Design system (all via common.py ADD_CSS — BASE_CSS/design reference untouched, overridden by cascade)
- **Teal ramp**: `--teal-50…800` (5 tints + 2 shades from #2f7d6c, hue 167° held constant). Light bg stays the signature #f0f9f8. Dark theme rebuilt as "same product at night": bg **#0c1620** (teal-tinted navy, no true blacks), surface #12222a, surface2 #0e1b23, borders #18262f/#2b4049, text #e2ecea, ramp lightness-inverted.
- **Accent split**: new `--accent` / `--accent-ink` / `--accent-soft` / `--on-accent` tokens carry ALL decorative colour (nav, pills, buttons, links, lens switch, play/wn/copy buttons, dash-links, method footer, mini-kpi default). `--blue` is now **state-only** (to-extract chips, identified status, data-gap cards) — light theme gets a real blue #2563eb back instead of the old teal-as-blue conflation.
- **WCAG AA (both themes, computed)**: light `--muted` #3d6b65→**#33625b** (6.1–6.9:1); light state colours darkened for text use: green #059669→#047857, amber #d97706→#b45309, red #dc2626→#b91c1c, purple #7c3aed→#6d28d9, orange #ea580c→#c2410c. Dark muted #7e9a94 (5.4:1+), accent #4fb39c (6.4:1+). Light jurisdiction tokens --iom/--gsy/--jsy unchanged; data-layer identity colours unchanged.
- **Typography**: scale 10/11/13/16/22/32; killed 9px in labels/chips/table-headers (obs-table th, lens-tag, opp-kind, t-group, gap-chip, rpill, sb-label, tl-year/label → 10px). Sources keep their 9px mono texture (.src-inline, .mk-s, .source-chip). Hero numbers: tile KPI 26→32px, tabular-nums + tighter tracking everywhere numeric (.t-kpi/.mk-v/.am-score/.sb-val/.num), labels given air (margin-top 7px, muted). Whitespace between sections: .section-label margin-top 28→44px (32 on mobile), .section-controls 20→40px.
- **Chart vocabulary** (audited every canvas + Sankey): jurisdiction colours = identity, never reassigned, always a second cue (3-yr series dashed, funding chart point styles, CAA corridor dashed); green/amber/red/blue = state only (savings recurring/non-recurring, waits severity, fee ladder, own-target ✓/✗ kept — semantic); everything else teal ramp: 06 workforce composition (was blue/red/violet/amber/green/slate → 6 ramp steps + dash/point second cues in line views + usePointStyle legend), 07 transfer-spend categories (was purple/green/amber → ramp), 05 FTE-GP line (was decorative red → teal-500 + dash + rect points, axis labelled "(dashed)"), 04 neutral budget bars (slate rgba → obsNeutral() muted teal), Sankey muted text → var(--muted). Shared helpers in SHARED_JS: `cssv() obsAxis() obsRamp(n) obsNeutral() OBS_DASHES OBS_POINTS obsAR() obsTicks() fmt1()`.
- **Positivity**: hub **Bright Spots strip** (renderBright, runtime from OBS): dental 6,463→~1,500; IoM deficit £31.1m→£14.9m (derived from mcFinance5yr, matches existing "halved from peak" copy); Shetland AM 360.5→222.2; WI 93% local care. `✓ improving` chips added where the existing copy already asserts improvement: 02 Shetland latest-rate card, 04 iomNote, 05 dental strip, 07 WI local-care card ("✓ benchmark"). NOT added for agency costs — no −50% series exists in the data layer (nearest: "reductions achieved, % not published"), declined to invent one.
- **Ethics visible**: unified chip set `.chip.derived/.toextract/.notpub/.improving` + dense-table variants `.gap-chip.toextract/.notpub/.derived`; **chip legend component** injected by emit() on every page above the methodology footer ("the observatory's honesty, worn openly"); methodology footer summary restyled in accent; Samaritans/Lifeline line on 02 wrapped in `.care-line` (soft green panel) — wording unchanged.
- **Ease of use**: 3-step first-visit whisper on hub (dismissible, localStorage `observatory-intro`); auto **sticky sub-nav** on pages with ≥4 visible section labels (built from .section-label text, IntersectionObserver highlight, sits under topbar via measured --topbarH); **focus tint**: topbar gradient + border take the focus jurisdiction colour (--focus-jc) and a focus pip (dot + short name, links to hub) sits in the top-right — set by obsPaintFocus() in boot + obsSetJur.
- **Touch (house rule 5)**: shared delegated tap handler → bottom "Basis & source" sheet (#srcSheet) for every [title] element and Sankey node/ribbon <title>; excludes links/buttons/[onclick]; closes on scroll/✕. Hub + 02 AM hover-trend gets tap-to-toggle (tapAm, no-op where hover exists). 03 impact badges gain onclick. Banner wording: "hover **or tap** any figure".
- **Mobile**: breakpoints 720/420 (+ existing 780/480). Topbar nav → horizontal swipe strip (no hamburger); hub tiles 1-col at ≤480, baselines fold beneath; stat strips 2-up at 720, 1-up at 420; kpi-matrix + scoreboards scroll INSIDE panels with **sticky first column** and mask-fade edge (.has-hscroll via obsScrollHints()); `.card-grid-6` (06 activity + card view) → scroll-snap rows at ≤720 (46% cards, 88% at ≤420) — **logged exception to house rule 1's fixed-columns clause on phones, per the brief**; charts drop to aspectRatio 1.6 + thinned ticks (obsAR/obsTicks/OBS_NARROW); ≥44px touch targets on coarse pointers; .source-chip wraps at ≤720 (was the only source of page-level h-scroll, found+fixed by probe).
- **Micro-polish**: global teal crosshair plugin on line charts (obsCrosshair, theme-aware); Chart.js defaults → Inter; `prefers-reduced-motion` respected (CSS transitions/animations off, sk-ribbon fade skipped, Chart.defaults.animation=false); **print stylesheet** (chrome/controls hidden, white bg, borders grey, cards break-inside:avoid); fmt1() helper added alongside fmtN.

## Files changed
`common.py` (tokens/CSS/SHARED_JS/emit — the bulk), `build_01` (whisper, bright spots, tapAm), `build_02` (care-line, chips, obsAxis, tapAm, scorecard chip classes), `build_03` (impact-badge onclick, 9→10px hint), `build_04` (obsNeutral, cssv state colours, improving chip, accent links), `build_05` (ramp spend categories, WI chips, chip classes, obsAxis), `build_06` (changelog v0.6/0.7/0.8 entries, accent), `build_07` (workforce ramp+cues, card-grid-6, chip classes, accent, obsAxis), `build_08` (dental improving chip, FTE teal line, obsAxis), `App.islands.jsx` (**dark THEMES matched to the new night ramp — NB this is the site shell, applies beyond the observatory; health muted/AA state colours updated; accent #2f7d6c unchanged**).

## Verification
v05 **102/102** (ONE expectation updated: "08 changelog 5 entries" → 8 — deliberate, v0.6/0.7/0.8 entries added to the public changelog), v06 **47/47**, theme **13/13**, compact **10/10**, smoke ×8 clean. Screenshots: all 8 pages × light/dark × 1280/768/390 (48) via headless chromium — **no horizontal page scroll anywhere** (asserted programmatically per shot); eyeballed hub/02/04/06 both themes + 390 mobiles. Note: headless fullPage screenshots drop canvas layers (blank charts) — use viewport-height capture; charts verified painted via getImageData.

## Session plumbing (next session read this)
- OneDrive→VM mount serves **stale-truncated copies of files rewritten from the host side** (capped at first-cached size; content refreshes, length doesn't). Symptom: "unterminated string" SyntaxError on builders. Workaround used: keep authoritative copies in the VM (~/ob), apply every common.py edit twice (host Edit + VM patch), never `cp` freshly-host-edited files from the mount; recover truncated files via mount-head + host-Read tail. VM-side writes (built pages) propagate fine.
- Library build scripts still carry `intelligent-sharp-bell` / `/tmp/ob` paths — sed to your session name and ~/ob as usual (per Session 7 note).

## Deploy note
observatory pages changed visually + App.islands.jsx dark/health themes changed → **redeploy ALL EIGHT islands files to public/islands/ AND src/App.jsx together** (the shell dark palette now matches the pages; shipping one without the other shows two different navies). manxcare-budget-sankey.html unchanged this session.

---

## Session 9 — 14 Jul 2026 · "source the metrics" data-fill pass (v0.8 → v0.8.1)

### Gap cells filled: 163 → 136 (27 closed, 2 corrections, ~10 notes sharpened)

**06 KPI matrix (Scottish island boards, from PHS open data — all values in `benchmarking/02_extracted_data/clinical_operational_kpi/scottish_islands_kpi_2025_26.csv`):**
- ED 4-hour FY2025-26 Type 1: ork 93.9% (8,465 att), shl 84.5% (8,733), wis 92.5% (6,372 — replaces the single-HIS-week 98.1%, old figure kept in basis)
- ED >12h FY2025-26: ork 0.06% (5), shl 0.19% (17), wis 0.02% (1)
- NOP ongoing waits >52wk at 31 Mar 2026 census: ork 7 (0.3/1k derived), shl 8 (0.3/1k), wis 0 — all three collapsed from Mar-2025 (136/46/6); basis = new-outpatient, nearest to IoM first-OP basis, flagged
- Cancer 62-day calendar 2025: ork 53.3% (16/30), shl 69.1% (56/81), wis 63.6% (68/107) — tiny-counts caveat verbatim from PHS
- Cancer 31-day calendar 2025: ork 79.2% (42/53), shl 94.5% (104/110), wis 94.4% (136/144)
- 28-day FDS + urgent-GP-≤6wk ork/shl/wis: re-chipped "no Scottish equivalent" (was wrongly "PHS publishes")
- Ambulance Cat 1 ork/shl/wis: re-chipped "SAS publishes — not yet extracted" (confirmed absent from PHS open data)
- Diagnostics 6-week + dental ork/shl: still blue, notes now name exact resource & why deferred

**02 popHealth matrix (`benchmarking/02_extracted_data/population_health/population_health_fills_2026-07.csv`):**
- LE: jsy m 81.3 · f 84.9 (2021-23); eng m 79.4 · f 83.3 (2022-24 ONS 10 Dec 2025); iom refreshed to f 84.4 (Mortality Report 2023)
- HLE: **jsy m 63.8 · f 61.2 — CORRECTION, cell wrongly said Jersey doesn't publish HLE**; gsy 60.2 (2022-24); eng m 60.9 · f 61.3 (2021-23)
- Infant mortality: gsy 0.7/1k (3-yr 2022-24); jsy "<5 deaths/yr, suppressed"; eng 3.9/1k (E&W 2023); **iom re-chipped RED not-published** (Mortality Report 2023 has no infant section; D4E FOI pending)
- MMR2 by 5: ork 94.8%, shl 86.7%, wis 86.1% (PHS FY to Mar 2026, with 6-yr series arrays), eng 83.7% (UKHSA COVER 2024-25)
- Screening eng: breast 70.6 uptake 24-25 · bowel 67.6 uptake 23-24 · cervical 68.8 coverage 23-24 (measures-differ flagged)
- IoM screening note corrected: the library "2025Cancer_Statistics.pdf" is a **Guernsey** FOI response, not IoM screening
- Scottish-island LE/HLE/suicide/infant/screening: still blue — sources located precisely (NRS 2022-24 LE workbook, NRS HLE Feb-2026, ScotPHO/NRS suicides, NRS VE tables, PHS screening reports) but all xlsx/PDF-only; web_fetch cannot render binaries. **Next session: drop these workbooks into the library or use the Chrome extension.**

### James mid-session asks (both shipped)
- **Children's dental table (05)** rebuilt per house rule 1: measures as row bands, 7 fixed jurisdiction columns (IoM first), kpi-matrix class (sticky first col on mobile), per-jurisdiction chips instead of the mushy "Others" column; NDIP (decay) and PHS participation named as extraction targets.
- **Micro-graphs:** new shared `obsSpark()` (inline SVG polyline + last-point dot, `var(--accent)`, <title> = labelled series) rendered by 02's matrix for any cell with `series`/`seriesLabels`. Live on the three MMR2 island cells. Convention: add series arrays to any cell → sparkline appears. Wider rollout (dependency ratios, GP FTE, budget vs outturn, dental list) queued.

### Method notes (important for reproducibility)
- PHS open data fetched WITHOUT curl (proxy 403) via CKAN dump paging: `datastore/dump/<resource>?offset=&limit=` (CKAN 2.8 ignores filters/q/sort; JSON API renders empty in web_fetch). Binary-search offsets to find recent rows; ~53KB per fetch.
- **OneDrive truncation bit hard**: every file host-rewritten in the v0.8 session (build_01-08, common.py, verify_v05.js) was stale-TRUNCATED in the VM mount (stat lies too). Recovered by splicing mount-head + host-Read tail; recovered copies written back VM-side (refreshes cache + persists). gen_data*.py were fine.
- Only pages 01/02/05/06 rebuilt this session (data is inlined per page; 03/04/07/08 embed the pre-fill OBS — none display the changed cells, but **rebuild all eight next session** once build_03-06 are recovered, and add the v0.9 changelog entry to 08 then (v05 expects exactly 8 entries — bump expectation together).
- Builders now sys.path ~/ob not /tmp/ob (stale /tmp/ob from an old session is root-owned).

### Verification
v05 **102/102**, v06 **47/47**, theme **13/13**, compact **10/10** (zero expectation changes), smoke ×4 on rebuilt pages clean. Gap-cell audit: 163 → 136, all filled cells carry basis + source; buckets unaffected. verify.js/verify2.js/verify_cards/verify_lens reference pre-rename filenames in their mount copies (stale/superseded) — did not run; reconcile next session.

### Deploy note
01/02/05/06 changed → redeploy those four to `public/islands/`. 03/04/07/08 unchanged this session. App.islands.jsx untouched.

### Session 9 addendum — manxcare-budget-sankey.html brought up to v0.8 look & feel
- Token blocks replaced with the observatory palette (night ramp `#0c1620`/teal, Manx Care light `#f0f9f8`/`#2f7d6c`), teal-ramp vars included for future use; `html:has(body.light)` bg updated.
- Page accent (`--gold`: play button, FY readout, slider, snapshot bars, footnote markers) is now the observatory teal — this also resolves the old ambiguity where the page accent and the *staff-costs* flow category shared the same gold. **All Sankey flow-category colours untouched** (orange = side-door money etc. — semantic, house rule).
- Time-series popup Chart.js colours now match obsAxis() greys/teals in both themes; `--on-accent` ink on the play button.
- Added no-flash theme boot right after `<body>` (same pattern as built pages), print stylesheet, `prefers-reduced-motion` guard.
- Verified: 3 inline script blocks `node --check` clean; jsdom smoke (d3 stubbed) no runtime errors. File edited VM-side (cache-safe). **Redeploy to site repo `public/` root (NOT /islands/).**

### Session 9 addendum 2 — Healthcare Explorer restyle + Reviews Tracker retirement
- **manx-care-benchmark.html** (new patched copy now kept in 70_dashboards/, like the sankey): dark theme moved from old slate (#0f172a) to the v0.8 night ramp (#0c1620 + teal surfaces); light theme was already Manx Care teal — state colours nudged to current common.py values (green #047857, amber #b45309, red #b91c1c) and muted aligned; --hc-accent → observatory accent (#4fb39c dark). Waiting-band legend chips + wkColor() now use theme-aware state vars instead of dark-only hexes. No-flash boot added (honours stored dark theme; page previously always flashed light). Print + reduced-motion added. Jurisdiction identity colours untouched. All 3 script blocks node --check clean; jsdom smoke clean. **Deploys to public/ root.** Data-layer unification remains roadmap item 2 — layout and inline data untouched.
- **manxcare-reviews-tracker.html RETIRED** (James's call, 14 Jul 2026): deployed page is byte-identical to the design reference and fully superseded by 03_governance (search/status/impact filters all present there; AM grid superseded by 02; IoM arrays extracted at build time). Replaced with a themed redirect stub → /islands/03_governance.html (meta refresh + JS, no parent hijack if iframed). App.islands.jsx: mcReviews route + keep-alive entry removed (menu entry was already absent). **manxcare-reviews-tracker_DESIGN-REFERENCE.html stays in the library — build dependency (common.py tokens + IoM reviews/actions source). Do not delete.**
- Deploy: manx-care-benchmark.html + manxcare-reviews-tracker.html (stub) → public/ root; src/App.jsx ← App.islands.jsx.

---

## Session 10 — 15 Jul 2026 · ScotPHO/NRS extraction pass (v0.9)

### Extracted (PHS open data, CKAN dump paging — no binaries needed)
- **Diagnostics by board, 31 Mar 2026 census** (resource 10dfe6f3 — the "deferred" chip from Session 9): % of list waiting >42 days: **ork 26.3% (83/315) · shl 19.3% (59/306) · wis 13.1% (76/582)**; MRI + ultrasound drive all three. Per-test detail in `benchmarking/02_extracted_data/clinical_operational_kpi/scottish_islands_diagnostics_2026-03.csv`. File layout cracked: fixed 1391-row series per test per board (85 months Mar19–Mar26; 14 bands pre-Sep20, 17 after), boards grouped by HBT, tests Endoscopy(Colono/Cysto/Lower/Upper)→Imaging(Barium/CT/MRI/US); Shetland has no Barium series and NO All-Endoscopy aggregate (Orkney has both) — so all three boards computed from the 8 individual tests for a consistent basis. 06 cells filled (ok:false — Scottish standard is no >6wk waits).
- **Cervical screening coverage FY2024/25** (KPI 1.1 resource eb5b53df, found via opendata.scot mirror which exposes full resource UUIDs — remember this trick): coverage 25-64 all-SIMD: **ork 63.5% · shl 63.4% · wis 59.1% vs Scotland 55.3%** (matches publication headline). All three islands ABOVE the national average. CSV in population_health/. 02 screening cells part-filled (blue chip retained — bowel/breast outstanding).

### Findings (recorded as findings, not numbers)
- **Breast screening statistics: PHS accreditation SUSPENDED Feb 2026** (data-quality errors; OSR letter 24 Feb 2026; latest edition still "to 31 Mar 2023", marked unreliable, next update note 25 Aug 2026). Recorded in manifest + 02 cell notes. Do NOT extract numbers from this series.
- **Dental participation re-scoped**: GDS practice-level open data (Mar 2026) exists but EXCLUDES salaried/PDS clinics that provide most island NHS dentistry — deriving board registration from it would mislead (house rule 6). Route = data request to phs.dental-info@phs.scot. 06 cell notes sharpened.
- NRS HLE 2022-24 is now **Official Statistics in Development** (ONS interim method; APS accreditation suspended) — old-method 2021-23 values NOT comparable; notes updated on ork/shl HLE cells. WI female HLE 64.5 (±13.4) extracted from report TEXT (council area = board footprint) into the wis cell (partial fill, chip retained).
- NRS Probable Suicides 2024 report Fig 4 qualitative extract: WI highest board rate / Orkney above avg (both wide-CI, not significant), Shetland lowest; Scotland 5-yr 2020-24 = 14.0/100k. In suicide cell notes pending workbook.

### Blocked → queued for James (download links messaged 15 Jul 2026; xlsx/zip can't come through web_fetch, Chrome extension not connected)
1. NRS LE 2022-2024 data.xlsx · 2. NRS HLE 22-24 data.xlsx · 3. NRS Probable Suicides 2024 tables.xlsx · 4. NRS Vital Events 2024 all-chapters CSV zip · 5. PHS bowel KPI report Apr 2026 xlsx → drop into mortality/scotland-island-boards/ (1-4) / benchmarking source docs (5). fetch_manifest.csv rows added (manual_needed). Next session: extract these, flip LE/HLE/suicide/infant cells + finish screening, then re-run gen_data7 → build_02.
- NDIP (children's dental) not on CKAN — remains xlsx target for the 05 children's table.

### Mechanics
- build_03/04/05/06 mount copies were stale-truncated as predicted; HOST copies were intact — recovered via host-side Read → VM-side rewrite (~/ob + mount). Builders now `sys.path` ~/ob via expanduser (no more /tmp/ob).
- gen chain rerun 3→7 (session paths sed'd); data layer 328,925 bytes, node --check clean. All EIGHT pages rebuilt (Session 9's outstanding item) + v0.9 changelog entry on 08; roadmap sco items updated to show extraction progress.
- OneDrive note: web_fetch of opendata.nhs.scot/statistics.gov.scot HTML+JSON renders EMPTY; CKAN /datastore/dump/<uuid> CSV works (~50KB/fetch, rate-limited ~4/min); statistics.gov.scot SPARQL dead via proxy (GET len-capped, POST unavailable); **opendata.scot dataset pages render fully and list every PHS resource UUID + direct CSV URL — use it as the resource directory.**

### Verification
v05 **102/102** (ONE expectation updated: changelog 8→9 entries — deliberate, v0.9 added), v06 **47/47**, theme **13/13**, compact **10/10**, smoke ×4 (02/03/06/08) clean. Gap-cell movement: 06 diagnostics ×3 blue→filled; 02 screening ×3 and HLE wis part-filled (chips retained — bucket honesty); benchmarking gap_register diagnostics row updated.

### Deploy note
Data layer + all eight pages changed → redeploy ALL EIGHT to public/islands/. App.islands.jsx, sankey, benchmark explorer untouched.

### Session 10 addendum — James dropped all five workbooks mid-session → FULL FILL
All remaining popHealth blues for the Scottish island boards are now real values (gap:False), from workbooks in `benchmarking/01_source_documents/uk_scotland/`, tabulated in `02_extracted_data/population_health/scottish_islands_pophealth_fills_2026-07-15.csv`:
- **LE at birth 2022-24** (abridged, Table 4): ork m 79.3 · f 82.8 · shl m 79.5 · f 83.9 (highest female of the six islands) · wis m 76.8 · f 80.8
- **HLE at birth 2022-24** (Table 3, OSiD): ork m 72.9 · f 74.1 · shl m 68.3 · f 70.8 · wis m 63.4 · f 64.5 (CIs huge — kept in basis)
- **Probable suicides 5-yr 2020-24 ASMR** (Table 3B): wis 22.5 (highest board in Scotland) · ork 19.8 · shl 10.6 (lowest) vs Scotland 14.0 — island CIs all overlap the national average
- **Infant deaths 2024** (VE Table 1.03): 0 in all three boards — displayed as counts, no rate derivable (house rule 6)
- **Bowel screening uptake May23-Apr25** (KPI Table 1): shl 73.6 (HIGHEST board in Scotland) · ork 70.0 · wis 67.6 vs Scotland 65.2 — screening row now bowel+cervical values with the breast suspension as the third slot's finding
gen_data7 re-patched + re-run (ext-7 only, it is the last extension), all 8 pages rebuilt again, v05 102/102 · v06 47/47 · theme 13/13 · compact 10/10, smoke 02 clean. fetch_manifest rows flipped manual_needed→fetched. Note for gap audit: 02 popHealth ork/shl/wis rows are now FULLY green-ink (no blue chips left in those columns except IoM/Jsy/Gsy gaps).
### Bright spot candidates for a future pass (NOT added — data-layer-only session)
Shetland: best bowel-screening uptake in Scotland; all three islands above Scottish average on bowel AND cervical. Candidate for hub Bright Spots strip next design session.

---

## Session 11 — 15 Jul 2026 · 04 Financial bug fix + tab-clutter cleanup

**Bug reported by James**: "the financial dashboard isn't working." Reproduced live (observatory.coalfinch.com/islands/04_financial.html): clicking any VIEW pill other than "Sources & Uses" blanked the entire page below the pill row.

**Root cause**: `build_04.py`'s CONTENT had an unclosed `<div class="lens-note">` inside the Sources & Uses tab panel. The stray `</div>` after the flags section closed `.lens-note` instead of `#fin-flows`, which left `#fin-flows` itself never closed — so `#fin-bva`, `#fin-sav`, `#fin-cap` and the page footer all ended up nested *inside* `#fin-flows` rather than as siblings. `setFinTab()` correctly un-hid the target panel, but hiding `#fin-flows` (now their ancestor) when switching away from it hid everything nested inside it too — including the panel just revealed. Confirmed via computed styles in a live tab (`parentDisplay: "none"`).

**Fix — also addresses James's design feedback** ("lots of unnecessary control and text… the tabs could be the only control here, and the VIEW should be tabs. This page should literally be sources & uses. Where did budget vs actual go, savings and per capita?"):
- Removed the `.filter-bar` VIEW pill row and `setFinTab()`/`fin-*` show/hide JS entirely — it was both the bug's mechanism and the redundant control.
- Sources & Uses / Budget vs Actual / Savings / Per capita are now flat, always-visible `.section-label` sections in one scroll, so the *existing* auto sticky sub-nav (`common.py`, built from `.section-label` text, ≥4 sections) becomes the page's only navigational control — same effect as the old pills, without the fragile display:none mechanic. Confirmed via shortTitle(): tabs now read FINANCIAL / SOURCES & USES / BUDGET VS ACTUAL / SAVINGS / PER CAPITA.
- "All five pots, side by side" and "The flags" downgraded from `.section-label` to `.section-label-inline` (same visual weight, no top margin) so they stay as in-page sub-headings under Sources & Uses rather than adding two more top-nav tabs — this is what was cluttering the tab bar before (it showed ALL FIVE POTS / THE FLAGS as if they were peer views).
- Removed the inaccurate line from the public-lens paragraph: *"None of this is fraud; all of it is money arriving outside the front door of the budget process, which makes it hard for you to see what your health system truly costs."* (James: not true — flagged for removal.) Rest of the public-lens paragraph (reserve draws / late allocations / brokerage / funding-formula overrun) is unchanged and still sourced.
- **The flags (9 finding cards) now hidden on mobile** (James: "content like 'The Flags' shouldn't exist on mobile"): new `.mobile-hide { display:none !important; }` utility added to `common.py`'s existing 720px breakpoint block, applied to the flags heading + `#flagStack`. Content remains in the DOM (desktop/tablet see it); doesn't affect the `04 flags present` verify_v05 check (jsdom doesn't apply media queries).
- Since panels are no longer display:none at load, the old "charts redraw on tab switch (hidden-canvas sizing)" workaround (`if (t==='bva'||t==='sav') drawCharts();`) is gone — `drawCharts()` runs once, unconditionally, on load, which is more robust (removes a whole class of Chart.js-sizing-inside-display:none bugs).

**Not done this session** (flagged, not attempted): a fuller "reliant on graphics, not text" mobile pass — condensing the lens-note prose blocks and Budget vs Actual / Savings / Per capita copy into more chart-first mobile layouts. James's note mentioned this; scoping it needs a design call (how much of the sourced caveat text can compress without breaking house rule 5/6) rather than a quick fix, so it's logged as a follow-up rather than guessed at.

**Verification**: `node --check` on all 4 extracted inline script blocks of the rebuilt 04_financial.html — clean. `smoke.js` on 04 — NO RUNTIME ERRORS, jurChips/flagStack/capStrip all POPULATED. `verify_v05.js` — 04-specific checks (clean, sankey present, flags present ×9, pots present ×5, no audit table) all pass (101/102 overall; the one unrelated failure, "06 clean", is a pre-existing jsdom `window.matchMedia` harness gap on a page untouched this session, not a regression). `verify_v06.js` similarly shows pre-existing failures confined to page 02 (untouched this session) for the same harness reason.

**Files changed**: `common.py` (+`.mobile-hide` utility in the 720px breakpoint), `build_04.py` (structural rewrite of CONTENT — see above; PAGE_JS: removed `setFinTab`).

**Deploy note**: only `04_financial.html` changed → redeploy that one file to `public/islands/04_financial.html` in the site repo (jc-bb07/reid-observatory). James is shipping this one manually (dev → GitHub → Vercel) rather than having it pushed directly this session.

---

## Session 12 — 16 Jul 2026 · Mobile simplify pass (public-only) + softened mortality wording

**Context**: this is the "fuller mobile pass" flagged as a follow-up at the end of Session 11 ("James's note mentioned this; scoping it needs a design call... logged as a follow-up rather than guessed at"). James: mobile is "way too text heavy... bring the graphics and important data elements front and centre," lock mobile to Public view only, and remove controls that overcomplicate the phone experience. Separately: soften the avoidable-mortality framing so it reads as neutral epidemiology rather than blaming the health system for deaths that may have other causes (patient choice, non-uptake, etc.) — James's example: "how many people die from preventable or treatable causes" instead of "causes that care should have prevented."

**House rule 4 compliance note**: the previous attempt at this pass (same session, earlier) was hand-edited directly into the 8 output HTMLs. That work was silently lost when Session 11's rebuild ran (`build_01..08.py` regenerate the HTML from scratch — hand edits never survive a rebuild). Redone properly this time, entirely through `common.py` and the per-page `build_NN.py` sources, per house rule 4.

**Mobile simplify — new `@media (max-width:780px)` block appended to `common.py`'s `ADD_CSS`** (plus a 480px tier):
- Public lens forced on mobile regardless of stored `observatory-lens`: `body[data-lens] [data-lens~="public"] { display:block !important }` overrides everything else; `.lens-switch` hidden.
- Onboarding whisper cut from a 3-step lecture to one line (step 1 — "pick your view" — dropped since the switch is gone; heading hidden; a compact `::before` line takes over).
- `.lens-note` clamped to 2 lines (1 at ≤480px) — the framing paragraph, not the number, was the thing making every page feel like a wall of text.
- `.compare-banner` hidden on mobile (methodology caveat, not a mobile-first read).
- `.chip-legend` keeps the coloured chips as a visual key but drops the `.cl-def` sentences.
- `.dash-links` (Hub) become 2-up icon tiles — number + name only, `.dl-desc` hidden; 1-up compact row at ≤480px.
- Governance's search/filter toolbar (`.filter-bar:has(#searchInput)`) hidden on mobile — politician-grade refine tool, not needed for a public glance at the full list. Confirmed via `:has()` that this does NOT touch 04/05's tab-style `.filter-bar` (content switchers, kept).
- `.card-controls` (Expand/Collapse-all on 03, Card/Matrix toggle + Composition/Growth/Share toggle on 06) hidden — all three already have sensible defaults (03 defaults to collapsed; 06's wfc chart defaults to a real "stack" view) **except** 06's Card/Matrix toggle, whose default is the dense scrolling matrix table — see below.
- Footer trimmed to the build line; the long source-list trailer hidden.
- Header trimmed (`.brand-sub` hidden, tighter gap), `.obs-nav` gets a fade-edge mask so the horizontal scroll reads as swipeable.

**`build_07.py` (→ `06_secondary-care.html`) PAGE_JS**: one line added after the initial render bootstrap — `if (matchMedia(max-width:780px).matches && compactView) toggleKpiView();` wrapped in try/catch (jsdom in this harness has no `matchMedia` at all, confirmed via `verify_compact.js`, hence the guard). Forces mobile's default to the friendlier Card view instead of the matrix table, since the toggle button that would let a mobile user switch is now hidden.

**Wording — `build_01.py` and `build_02.py` CONTENT**:
- Hub Public-lens intro: "how many people die from causes that care should have prevented... what's still not fixed" → "how many people die from preventable or treatable causes... what's still outstanding."
- 02 Public-lens intro: "causes that prevention... or timely treatment should have stopped" → "preventable or treatable causes — ones that public health measures... or timely treatment can reduce."
- 02 Preventable-vs-treatable panel: "public-health failure space... healthcare failure space... care should not let become fatal" → "mainly reducible through public health action... mainly reducible through timely, effective healthcare." Matches the OECD/Eurostat preventable/treatable vocabulary already cited elsewhere on the page. Checked the other 6 pages' Public-lens text and the wider data layer for the same pattern — no other instances in the site's own voice (matches inside `ALL_ACTIONS[].text`/`notes` are verbatim source quotes from review PDFs and were left untouched).

**Mechanics — mount staleness (recurring issue, see Session 9/10 notes)**: `common.py`, `build_07.py` and `data/observatory_data.js` all hit the documented stale-mount problem after editing — `common.py` and `build_07.py` were genuinely truncated in the VM-side view (host-side `Read` had the correct, complete content); `observatory_data.js` turned out to be trailing null-byte padding after a complete, valid file rather than truncation, fixed with a straight trim. All three recovered via host-side content → new filename → delete old → rename (the tombstone workaround), not by editing the stale copy in place. One transcription error caught during recovery: a curly apostrophe in `common.py`'s `obsPaintFocus` tooltip string got flattened to a straight one while retyping, which would have closed the JS string early — caught by `node --check` before it shipped.

**Verification**: full rebuild (`gen_data*` untouched, data layer only null-trimmed) → `build_01.py`..`build_08.py`, all 8 write clean. `verify_v05.js` **102/102**, `verify_theme.js` **13/13**, `verify_compact.js` **10/10** (all match or exceed the last recorded baseline). `verify.js` and `verify_v06.js` still reference an `hnaTable`/`hnaWrap` element that no longer exists anywhere in the build sources (`grep` across all `build_*.py` and `common.py`: zero hits) — pre-existing drift from before this session's history, not a regression; nothing in this session touched WI HNA rendering. `node --check` on all 4 extracted script blocks of all 8 rebuilt pages — clean.

**Files changed**: `common.py` (+mobile-simplify CSS block in `ADD_CSS`), `build_01.py` (wording), `build_02.py` (wording ×2), `build_07.py` (+mobile card-view-default JS), `data/observatory_data.js` (trailing null bytes trimmed — no content change).

**Deploy note**: all 8 island pages changed (common.py affects every output) → redeploy all 8 to `public/islands/`. James is shipping manually (dev → GitHub → Vercel) per his usual flow.

---

## Session 12 — 15 Jul 2026 · Card decluttering, hub/public-health/governance/financial trims, global chip-legend removal

Two rounds of James's UI/UX feedback, actioned together; all eight pages rebuilt and redeployed at the end.

### Round A — scorecard readability, financial header cleanup, hover explanations
- **`build_02.py` Population health scorecard**: m/f values now reliably line-break (`.split(' · ').map(esc).join('<br>')` instead of one run-on string). Added `PHSCORE_TIP` lookup + `title` tooltip on each row's metric name giving a plain-language explanation (life expectancy, healthy life expectancy, suicide mortality, infant mortality, immunisation, cancer screening) — same hover convention as the KPI dials. Basis/source moved from a visible line into the cell's `title` tooltip.
- **`common.py`**: `.obs-nav a.here` restyled from a filled pill to plain bold ink (`color: var(--accent-ink); font-weight: 700;` — no background/padding change), removing the "pill" look James flagged on the top nav.
- **`build_04.py` Financial**: removed the redundant on-page section heading ("04 · Financial — …") and its two-box lens-note duplication (per James choosing "both" via clarifying question) — page now opens straight into the compare-banner. `pctBarPlugin(pluginId, datasetIndex, pcts)` added: draws bold white "+n.n%" labels near the base of the "actual/delivered" bar, used consistently on Budget vs Actual (IoM), Crown Dependencies, and (at this point) Savings. Per-capita cards restructured: total expenditure shown large at the top of each card, £/head derived beneath, basis/source moved to a `title` tooltip — cards no longer carry a separate visible caption line.
- **`build_07.py` (Secondary care) "The dials"**: `explainFor(metricName)` wired into `renderMatrix()`'s row-title `title` attribute so every KPI row gets a plain-language hover explanation, matching the scorecard convention above.
- **Chip legend**: `CHIP_LEGEND` blanked to `""` in `common.py` (still referenced by `emit()`, now a no-op) — first step, made permanent in Round B.

### Round B — hub simplification, WI tab removal, MIAA text, financial trims, per-capita reorder, card decluttering
- **Global (`common.py`)**: `CHIP_LEGEND` block fully removed from the methodology footer text (was already blanked in Round A; the surrounding "Chip legend" paragraph is now gone too, not just its grid). `opportunitiesFor()`'s `challenge` opportunity kind (built from `rejected` review actions) removed entirely — no more "N recommendations rejected" cards anywhere in the suite. `LIB` path made portable (`OBS_LIB` env var, else resolved relative to `common.py`'s own location) so the build no longer depends on a hardcoded prior-session path.
- **`build_01.py` Hub**: `LENS_KINDS.politician` no longer includes `'challenge'` (list now `['accountability','transparency']`); politician-lens blurb text trimmed to match. `renderBright()` (Bright Spots) rewritten — card labels shortened to one line each (e.g. "IoM NHS dental waiting list — down ~77% in six months"), the visible `.bc-s` source-citation line removed from the card body and folded into a `title` tooltip on the card link instead.
- **`build_02.py` Public health**: Western Isles HNA tab removed completely — `#hnaWrap` block, its `#hnaNote` lens-note, `renderHna()`, `renderHnaVisibility()` and the `obsOnJur` hook that called it are all gone; the page now runs straight from the demography strip to the footer. The "Read this first:" `.compare-banner` box (which consumed a full-width row) replaced with an inline `ⓘ read this first` hover span appended to the chart's section-label-inline, carrying the same basis text as a `title` tooltip — recovers the vertical space without losing the caveat.
- **`gen_data5.py` + `observatory_data.js`**: both MIAA Governance review descriptions shortened. Manx Care-commissioned review: now "Commission Jul 2025 on Manx Care's own Board: 10 strategic actions covering mandate clarity, DHSC strategy co-production and financial reporting. ~51 tactical actions in Appendix O; per-action statuses not published." DHSC-commissioned review: now literally James's example text — "Commission Jun 2025 on DHSC/Manx Care arrangement: arm's-length model, blurred accountability and misaligned funding. \"Strengthened Partnership\" approach selected by Tynwald, Feb 2026." Because re-running `gen_data5.py` alone would truncate and drop Extensions 6/7 appended after its marker in `observatory_data.js`, the already-generated data file was hand-patched to match instead of regenerating the whole chain — `gen_data5.py`'s source was still updated for correctness on any future full regen.
- **`build_04.py` Financial**: removed the "Sources & Uses — where each island's health pound comes from, and where it goes" section-label wording and its full public/politician/management lens-note block (the ribbon-reading explainer); replaced with a short, unlabelled `Sources & Uses` section heading (kept short rather than deleted outright so the auto sticky sub-nav still has a wayfinding entry for it). Per-capita cards (total spend + £/head) moved from their old position near the bottom of the page to directly under the caveat banner, ahead of Sources & Uses — highest-impact numbers now load first. Removed the `%` label from the Savings chart (`savPct`/`pctBarPlugin` call deleted from `savChart`, dataset label reverted to plain "Delivered — recurring") per James's instruction to drop it from that one chart only; Budget vs Actual (IoM) and Crown Dependencies keep their `%`-on-bar labels.
- **Primary/Secondary/Tertiary card decluttering** (James: "move data governance notes to Methodology and Data Governance section to make cards higher impact and increase clarity"): across `build_08.py` (Primary), `build_07.py` (Secondary) and `build_05.py` (Tertiary), every `.mini-kpi` card's basis/source text (`.mk-s`, and pure-source `.mk-l` lines) was moved from an always-visible line into the card's `title` tooltip, leaving only the headline value and (where genuinely descriptive, not just a citation) a short label visible. Touched: `renderFeeLadder`, `renderGpComparators`, `renderGpPerf`, `renderDental` (build_08); `renderActivity`, `jurCard`, `renderWorkforce` (build_07); `renderKpis`, `renderTertDetail`, and the Remoteness exposure grid's duplicate visible-note divs, including the Population Served and Scheduled Flight Time header rows (build_05). Every relocated string still round-trips through the shared `showSrcSheet()`/`#srcSheet` tap-equivalent (house rule 5) since it's still a `title` attribute — nothing lost on touch devices.

### Deploy / verification
All eight pages rebuilt from a scratch mirror of the build scripts + data layer (this session's sandbox has a persistent divergence between the file-edit tools and the shell's mounted view of the OneDrive folder — edited files, and any data file ever touched by those tools, read back through the shell with scattered spurious null bytes; stripping literal `\x00` bytes from the shell-side read reliably recovers byte-correct source, confirmed by diffing against known text before rebuilding). All `build_0N.py` ran clean; all 4 inlined `<script>` blocks per page (32 total) passed `node --check`; targeted greps confirmed the chip-legend text, the WI HNA block, the Financial "Sources & Uses" prose block, and the Savings `%`-label plugin are gone from their respective pages, and that the MIAA text, workforce hover tooltip and nav-pill destyle landed as written. All eight HTML files copied back to the real `70_dashboards/` folder with byte-for-byte size verification.

**Files changed**: `common.py`, `build_01.py`, `build_02.py`, `build_04.py`, `build_05.py`, `build_07.py`, `build_08.py`, `gen_data5.py`, `observatory_data.js` (hand-patched, not regenerated).

**Deploy note**: because `common.py` changed (chip-legend removal is global, consumed by every page's `emit()` call), all eight pages were rebuilt and redeployed this session, not just the ones with direct content edits.

---

## Session 12 — 15 Jul 2026 · declutter pass (02 scorecard, 04 header/bars/cards, 06 dial tooltips)

**Requested by James** (reviewing screenshots of 02/04/06): reliably line-break multi-value cells (e.g. `m 79.4 · f 84.4`) onto separate lines; lean harder on hover + the Methodology & data governance footer instead of always-visible small print; add plain-language hover explanations on row/metric titles; remove redundant header furniture on 04; put the % variance on budget-vs-actual bars at the bottom of the bar, larger, white; give Crown Deps and the Scottish-board savings chart the same % convention; put total expenditure + per-capita at the top of the per-capita cards with the rest pushed to hover.

**`common.py`**:
- `LIB` was hardcoded to a now-dead session sandbox path (`/sessions/focused-keen-archimedes/mnt/data-library`) — every prior session's builders only ran because someone patched this by hand or ran from `~/ob`. Replaced with `os.environ.get('OBS_LIB') or <three parents of this file>`, so `common.py` resolves `data-library/` relative to its own location (or via `OBS_LIB` override) and the build works from a fresh session/checkout without hand-patching.
- `CHIP_LEGEND` (the standalone "How to read the chips" box, sitting just above the Methodology footer on every page) folded into `METHODOLOGY_FOOTER` as a "Chip legend" paragraph; `CHIP_LEGEND` itself is now `""`. One fewer always-visible box per page, no information lost — it was already low-traffic footer content.
- `.obs-nav a.here` (current-page highlight in the top nav) de-pilled: dropped the tinted background, kept bold + accent-ink text. Was duplicating the big on-page section title on every page.

**`build_02.py`** (Population health scorecard):
- `renderPhScore()`: cell values are now split on ` · ` and joined with `<br>` before rendering, so any multi-value cell (`m 79.4 · f 84.4`, `bowel 70.0 · cervical 63.5`, etc.) reliably wraps onto separate lines regardless of column width — not just when the browser happens to wrap.
- Removed the always-visible basis/source caption under every cell (`<div style="font-size:9px...">`); that text now lives entirely in the `title` attribute (hover on desktop, tap-to-reveal via the existing `#srcSheet` house-rule-5 mechanism on touch). Table is visibly lighter; nothing is lost, it's one hover away.
- Added `PHSCORE_TIP`, a small lookup of plain-language explanations for each of the six scorecard rows (life expectancy, healthy life expectancy, suicide mortality, infant mortality, immunisation, screening), wired as a `title` on the row/metric name cell — same hover/tap convention as everything else on the page.

**`build_04.py`** (Financial):
- Section header shortened from "04 · Financial — budgets, savings, and where the money actually flows" to just "Budgets, savings, and where the money actually flows" (the "04 · Financial" part duplicated both the top-nav label and the browser tab title).
- Removed the second "How to read the ribbons" `compare-banner` box that sat directly under the Sources & Uses lens-note — it repeated the proportional-split/DERIVED convention that's already stated in the one-line legend right above the Sankey (`hover any node or ribbon for its source · orange edge = the story · DERIVED = calculated, not reported`), which now also carries the fuller explanation as a hover tooltip. Kept the lens-note itself (the public/politician/management tabbed box) — that's the house-rule-3 mechanism, not redundant copy.
- New `pctBarPlugin(id, datasetIndex, pcts)` Chart.js plugin: draws the % variance in bold 13px white text near the *bottom* of the bar (was 10px red text floating just *above* the bar, IoM chart only). Applied consistently to all three budget/outturn bar charts on the page: IoM Budget vs Actual (overrun %), Crown Dependencies (variance % per island), and the Scottish-board Savings chart (% of target delivered) — same convention everywhere the page compares a plan against an outturn.
- `renderPerCapita()` cards rebuilt: headline is now **total expenditure (£Xm)** stacked above **spend per head (£X, derived)** — previously only per-head was shown as the headline. Basis, budget-per-head, population and sources moved out of the always-visible `.mk-l`/`.mk-s` lines into a single `title` tooltip per card, so the card itself only shows jurisdiction + the two topline numbers.

**`build_07.py`** (Secondary care / "The dials"):
- The default compact KPI matrix (`renderMatrix()`) already had a per-jurisdiction hover with basis+source on every cell, and the card view already showed a plain-language explanation per metric (`explainFor()` / `OBS.kpi[].explain`) — but the compact grid's row/metric-name cell had no such explanation. Added `title="' + esc(explainFor(row.metric) || '') + '"` to that cell, reusing the existing `explain` text already written for the card view, so hovering a KPI row title in the default grid view now gives the same layman's one-liner.

**Open item — "pill" on 04**: James also flagged a "pill title" as redundant alongside the two boxes. I confirmed and removed the two boxes and shortened the section title; for the pill I asked James directly and he confirmed **both** the top-nav current-page highlight *and* the on-page section heading — both addressed above (nav highlight de-pilled site-wide in `common.py`; heading shortened on 04 specifically, other pages' headings untouched pending any further request).

**Verification**: `python3 -c "import ast; ast.parse(...)"` clean on all three edited builders; ran all three builds against a scratch mirror of the data-library (session sandbox had a stale/lagging view of files just edited via the file tools — builds were run against a verified-fresh copy, then the output HTML copied back and byte-count-verified against the source of truth); `node --check` clean on all 4 extracted `<script>` blocks per rebuilt page (12 total); grepped rebuilt output for every marker above (`PHSCORE_TIP`, `pctBarPlugin` ×4, `explainFor(row.metric)`, chip-legend box absent, `sankeyNote` absent, shortened header present) — all present exactly once/as expected.

**Files changed**: `common.py` (LIB portability fix, CHIP_LEGEND fold-in, nav-pill destyle), `build_02.py`, `build_04.py`, `build_07.py`. Regenerated: `02_public-health.html`, `04_financial.html`, `06_secondary-care.html`.

**Not done this session**: did not touch 01/03/05/08 — none were flagged, and the shortened-header treatment on 04 was scoped to the page James reviewed rather than applied site-wide.

**Deploy note**: three files changed → redeploy `02_public-health.html`, `04_financial.html`, `06_secondary-care.html` to `public/islands/` in the site repo.

---

## Session 13 — 1 Aug 2026 · RCS General Surgery review released via FOI 5668853 — 'rcs' stub flipped to tracked

**Trigger**: James's FOI request (RCS-FOI-ManxCare-General-Surgery.docx, sent 30 Jun 2026) was answered by Manx Care 22 Jul 2026 (ref 5668853), releasing the redacted Invited Service Review Final Report (Royal College of Surgeons of England, General Surgery at Noble's Hospital) plus the RCS Review Action Plan, two Surgical Services Review programme documents, and three Manx Care Board (Private) paper excerpts (10 Dec 2024, 10 Sep 2025, 13 May 2026). Source pack collected to `10_health-social-care/manx-care/reviews-inspections/rs review/` (9 PDFs).

**What changed**: the `'rcs'` review in `manxcare-reviews-tracker_DESIGN-REFERENCE.html`'s `REVIEWS` array was a `foi:true` stub (published:null, "FOI request submitted — awaiting response"). Per the mcgov precedent (Session 11-ish, `gen_data5.py`), left the DESIGN-REFERENCE.html entry untouched and added `gen_data8.py` — a new extraction CSV (`rcs-general-surgery-review-2024-25_recommendations.csv`, 13 rows, same 10-column schema as `manxcare-governance-review-2025_strategic_actions.csv`) plus an `Object.assign` patch flipping `stub:false, foi:false, published:true` and setting date/what/author/commissioner/color/url. Unlike the CSV-only loader pattern (`gen_data.py`'s `load_actions()`, which carries no impact/objections columns), `gen_data8.py` hand-scores `impact` (scope/risk/urgency) and attaches `objections` for 3 of the 13 actions via a small in-script lookup, matching the native IoM entries' convention rather than the plainer non-IoM territory CSVs.

**The 13 actions** map to the RCS Final Report's 12 numbered recommendations (§4.1 urgent patient-safety recs 1-8, §4.2 service-improvement recs 9-12) one-to-one, plus a 13th for the financial-governance/disciplinary strand the FOI response withheld under s25(2)(b). Statuses cross-referenced between the original Nov/Dec 2024 action plan (risk ratings, initial target dates) and the 13 May 2026 Board update (current progress per its "12 broad areas," which map cleanly onto the 12 numbered recs): 7 in-progress, 4 partial ("Behind trajectory" per the Board's own May 2026 self-assessment — clinical strategy/vascular/urology, constrained by the Tynwald election period), 1 completed (change of Clinical Director — new CD in post, appointed 1 Nov 2025 from outside the department), 1 unknown (financial governance / disciplinary — Manx Care confirmed a GMC referral is under investigation but declined to disclose investigation counts, further referrals or any overpayment total). 3 objection flags: initial consultant resistance to the RCSE findings (RCS1), the Board's own "Behind trajectory" rating on strategy work (RCS4), and the FOI s25(2)(b) non-disclosure on disciplinary/financial follow-up (RCS13) — kept honest per house rule 5 rather than smoothing over the gap.

**`gen_data8.py` LIB resolution**: written portable from the start (`OBS_LIB` env var, else 3-parents-up of the script's own location) per the Session-12 fix to `common.py` — does not repeat `gen_data5.py`/`gen_data6.py`/`gen_data7.py`'s hardcoded dead session paths.

**Rebuild**: `gen_data8.py` run against a copy of `observatory_data.js` (appends the `EXTENSION 8` marker block, matching the `gen_data5.py` append-at-marker pattern — re-running is idempotent and does not disturb Extensions 5-7). `build_03.py` and `build_01.py` rebuilt (only these two touch `ALL_REVIEWS`/`ALL_ACTIONS` content that changed; the other 6 pages' inlined data layer is identical byte-for-byte since `common.py`/`build_0N.py` were untouched — confirmed by a from-scratch sanity rebuild against the *unmodified* `observatory_data.js`, which reproduced the live `01_index.html`/`03_governance.html` byte-for-byte before the RCS patch was applied, then diffing that sanity build against the RCS-patched build showed exactly one added block (the `EXTENSION 8` IIFE) and nothing else in either file).

**Verification**: `node --check` clean on `observatory_data.js` and all 4 inlined `<script>` blocks each on `01_index.html`/`03_governance.html`. `smoke.js` on both pages: all expected DOM containers populated (`glanceTable`, `reviewsStack`, etc.), no runtime errors. Targeted jsdom check on the `rcs` card confirms: `✓ Published` badge (previously `? Unclear`), no `stub-item` class, 13 action rows (status breakdown 7 in-progress / 4 partial / 1 completed / 1 unknown, matching the CSV), 3 objection badges, report link resolves to the FOI-portal URL, zero runtime errors. Hub `glanceTable` bucket check: IoM row now reads "10 (7 pub · 2 stub/FOI)" (was 6 pub · 3 stub/FOI) with the 3rd non-tracked review (Deanery EQI, `published:false`) unchanged — 7+2+1=10, buckets still sum (house rule 5). Did not run the full `verify_v05.js`/`verify_theme.js`/`verify_compact.js` suites (they require all 8 rebuilt pages in one directory and check site-wide nav/theme/mobile concerns untouched by this session) — scoped instead to `smoke.js` + the targeted RCS/glance checks above, since only 2 of 8 pages were rebuilt and nothing outside the reviews/actions data layer changed.

**Files changed/added**: `10_health-social-care/manx-care/reviews-inspections/rcs-general-surgery-review-2024-25_recommendations.csv` (new), `70_dashboards/data/build/gen_data8.py` (new), `70_dashboards/data/observatory_data.js` (Extension 8 appended), `01_index.html`, `03_governance.html`. `manxcare-reviews-tracker_DESIGN-REFERENCE.html` and `manxcare-reviews-tracker.html` (the standalone redirect stub) left untouched — the redirect has no review-specific content to update; the tracked data lives in `03_governance.html` per the existing division of labour.

**Deploy note**: `01_index.html` and `03_governance.html` changed → redeploy both to `public/islands/` in the site repo when James next ships. The other 6 island pages are unaffected.

---

## Session 13 addendum (same day) — RCS incident/SUI/never-event background + Gent cross-review theme

**Trigger**: James caught two things reviewing Session 13's work: (1) the Gent bed-capacity report's "Final Thoughts" section names the same behavioural/governance dysfunction RCS and Deanery separately found, and asked where that pattern should be drawn out; (2) whether the RCS document's incident/SUI/never-event background had been captured — it hadn't. First-pass extraction (Session 13 main entry) read the Recommendations section (§4) and the board updates but missed §1 (Introduction/background) and Appendix C's incident table.

**What was missed and added**: RCS Final Report p3-4 states the service recorded 1,456 incidents in the two years before the review (17 deaths/expected deaths, 20 severe/permanent-harm cases), 15 SUIs (9 "Finally Approved," 1 awaiting, 5 still being reviewed), and 1 "never event" still under review at the Oct 2024 visit — none of it referenced in the recommendations or the subsequent board papers collected. Added as **RCS14** (14th action, status `unknown` — no board paper in the collection reports the never event's outcome or confirms the remaining SUIs closed) and folded the headline stats into the `rcs` review's `what` summary, which previously led with culture/PA framing only.

**Cross-review theme**: pulled the Gent report's two relevant passages verbatim (§5.8 "ulterior motives," §7.0 "trust, discretionary behaviours, questionable Consultant job plans, silo-planning and circumvention of normal governance systems... ungovernable") — both explicitly flagged by the reviewer as outside his formal 8-recommendation brief, so **not** added as a fake G9 (G1-G8 stay a clean 1:1 map to Gent's actual recs). Instead: (a) a new `OBS.analyticalNotes` entry ("One unprompted theme, three independent reviewers," iom/page 03) making the Deanery/RCS/Gent pattern explicit — this is what that mechanism exists for; (b) the `gent` review's `what` field extended with the "ungovernable"/job-plans quotes; (c) RCS1's `notes` field gets one added sentence cross-referencing the Gent quote as independent corroboration.

**Implementation**: `gen_data8.py`'s CSV gained a 14th row (RCS14) and its `IMPACT` dict a matching entry (scope 2/risk 3/urgency 3 — never event + deaths score at the top of the scale used elsewhere). Re-running `gen_data8.py` cleanly replaced Extension 8 in place (marker-truncate-and-rewrite, same pattern as `gen_data5.py`) rather than duplicating RCS1-13. New `gen_data9.py` layers the narrative-only changes (review `what` fields, the analytical note, RCS1's notes addendum) as a separate extension appended after 8 — kept as its own file rather than folded into 8 so a future session can re-run either independently.

**Tooling note for future sessions — stale `device_stage_files` reads**: verifying the prior commit by re-staging `observatory_data.js`/`03_governance.html` initially showed the *pre-edit* content (byte-identical to the original, despite `device_commit_files` having reported success and the directory listing showing the correct new size/mtime). Reading the same paths directly through the mounted filesystem (`device_bash` against `/sessions/<id>/mnt/observatory/...`, once the folder had been granted) showed the correct, fully-committed content immediately — confirming the write itself was correct and the discrepancy was a stale read-cache specific to `device_stage_files`, not data loss. This is the same family of issue as the previously-documented OneDrive sync-lag/null-byte problems. **Lesson: after a same-session write-then-verify, don't trust a `device_stage_files` re-read of a just-modified path — verify via `device_bash` on the direct mount instead**, or treat a stale-looking re-stage as inconclusive rather than as evidence the write failed.

**Verification**: `node --check` clean on `observatory_data.js` and all 4 script blocks on both rebuilt pages. Targeted DOM checks: `rcs` card now shows 14 action rows (status breakdown 7 in-progress / 4 partial / 1 completed / 2 unknown), `gent` card still exactly 8 action rows (G1-G8 unchanged), RCS1's rendered notes include the Gent cross-reference sentence, the Hub's `notesStack` renders the new analytical note under the default (IoM) focus jurisdiction. Diffed both rebuilt pages against the Session-13-main-entry baseline (itself already diff-verified against the pre-RCS original): exactly one contiguous added block in each file, nothing else touched. All six files (CSV, `gen_data8.py` updated, new `gen_data9.py`, `observatory_data.js`, `01_index.html`, `03_governance.html`) re-verified byte-for-byte (md5) on the real device filesystem after commit, not via the unreliable stage-cache.

**Files changed/added**: `10_health-social-care/manx-care/reviews-inspections/rcs-general-surgery-review-2024-25_recommendations.csv` (14th row added), `70_dashboards/data/build/gen_data8.py` (IMPACT dict entry for RCS14), `70_dashboards/data/build/gen_data9.py` (new), `70_dashboards/data/observatory_data.js`, `01_index.html`, `03_governance.html`.

**Deploy note**: same two files as the main Session 13 entry — `01_index.html` and `03_governance.html` — redeploy both when James next ships.

---

## Session 13 addendum 2 (same day) — toned back individually-identifying content

**Trigger**: James asked to tone back anything potentially damaging to individuals, specifically citing the GMC referral detail.

**Removed/generalised**: RCS13's notes previously read "One consultant surgeon was referred to the GMC by a patient following a complaint that also involved HSCOB; the GMC has opened an ongoing investigation" — sourced from a Manx Care Board (Private) paper, i.e. more specific than what Manx Care itself chose to disclose (their FOI response explicitly withheld this category of detail under s25(2)(b)). In a department of 4 substantive consultants, "one consultant... referred by a patient... GMC and HSCOB" narrows enough to be identifying even without a name. Reworded to: "Manx Care's own board papers indicate disciplinary/regulatory and financial-governance follow-up connected to the review remained ongoing as of May 2026" — the fact that follow-up is happening stays (public-interest relevant, matches Manx Care's own "appropriate action... being taken" framing), the specific pathway (which regulator, whose complaint, which second body) does not. Same edit applied to the `rcs` review's `what` summary ("and a GMC referral followed" → "with disciplinary/regulatory follow-up ongoing") and RCS13's objection reason in `gen_data8.py`. RCS3's notes similarly softened — "most of which had fallen on one consultant doing 'excessive locum on-calls'" → "concentrated unevenly across the consultant body" (same underlying finding, no longer narrows to one identifiable person's workload pattern).

**Left as-is, flagged for James rather than changed unilaterally**: RCS2's notes still carry the RCS report's own verbatim quote — "some consultants were being paid twice for the same time or that there was manipulation of the system" — plural, not identifying, and it's the review's own already-redacted wording (RCS/Manx Care chose that phrasing when releasing the report). RCS5 still says "ahead of the current vascular surgeon's retirement" — an operational succession-planning fact, not a conduct/disciplinary matter, so a different category of sensitivity. Neither removed pending confirmation these are also in scope.

**Verification**: re-ran `gen_data8.py`/`gen_data9.py` (marker-truncate-and-rewrite, no duplication), `node --check` clean, grep sweep for "GMC", "HSCOB", "one consultant surgeon", "by a patient" across `observatory_data.js` and both rebuilt pages returns nothing tied to the RCS review (two unrelated pre-existing "GMC" hits elsewhere on the site, re: appraisal/revalidation, untouched). Diffed both rebuilt pages against the pre-Session-13 baseline: still exactly one contiguous changed block per file. All three touched artefacts (`observatory_data.js`, `01_index.html`, `03_governance.html`) re-verified byte-for-byte (md5) on the real device filesystem after commit.

**Files changed**: CSV, `gen_data8.py`, `gen_data9.py` (in place, no new extension number — same-day correction to this session's own additions), `observatory_data.js`, `01_index.html`, `03_governance.html`.
